{
  "name": "tbd-tender-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Telegram bot (Cloudflare Worker) for verified large-diameter-pipe (TBD) tenders",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.95.0"
  }
}
