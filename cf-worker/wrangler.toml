name = "tbd-tender-bot"
main = "src/index.ts"
compatibility_date = "2025-01-01"

# Хранилище подписок и дедупликации.
# id подставится после команды `npx wrangler kv namespace create TENDER_KV`.
[[kv_namespaces]]
binding = "TENDER_KV"
id = "REPLACE_WITH_KV_ID"

# Фоновый опрос новых тендеров — каждые 30 минут.
[triggers]
crons = ["*/30 * * * *"]

[vars]
PROVIDER = "mock"
OKPD2_PREFIXES = "24.20"
KEYWORDS = "труб,тбд,большого диаметра"
ONLY_WITH_WINNER = "true"
POLL_LOOKBACK_DAYS = "7"
MAX_RESULTS_PER_QUERY = "25"
# Имитация (временный предпросмотр формата): рабочее время по будням, МСК.
SIM_TZ_OFFSET = "3"
SIM_HOUR_START = "9"
SIM_HOUR_END = "19"
SIM_PROBABILITY = "0.66"
# Seldon.API — при переходе на реальные данные поставьте PROVIDER = "seldon"
# и заполните filterId (id сохранённого фильтра ТБД из раздела «Контракты»):
# SELDON_BASE_URL = "https://apitorgi.myseldon.com"
# SELDON_FILTER_ID = "1386962"
#
# Секреты (НЕ храните в этом файле!) задаются командами:
#   npx wrangler secret put BOT_TOKEN
#   npx wrangler secret put WEBHOOK_SECRET
#   npx wrangler secret put SELDON_LOGIN
#   npx wrangler secret put SELDON_PASSWORD
